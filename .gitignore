# Do NOT commit the 465 MB candidate pool — place it in ./data/ locally
data/candidates.jsonl
data/candidates.jsonl.gz

# Note: submission.csv / submission.xlsx ARE committed so reviewers can see the
# result without running the ranker. They are regenerated by rank.py.

# Python
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
